"""
Smart Campus Assistant backend package.

This module marks the `app` package and exposes the FastAPI application
instance for ASGI servers (e.g. `uvicorn app.main:app`).
"""

from .main import app



